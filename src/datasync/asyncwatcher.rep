POD TableEvent(QString table, QString connection)

class AsyncWatcher
{
	PROP(QList<TableEvent> activeTables={} CONSTANT)

	SIGNAL(tableAdded(const TableEvent &event))
	SIGNAL(tableRemoved(const TableEvent &event))

	SLOT(void activate(const QString &name))
};
